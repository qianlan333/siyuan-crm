# 运营闭环二级页收敛设计

- 顶部只保留“已循环轮次”和四阶段“本次循环进度”。
- 左侧只保留“群发数据明细”“执行策略文档”“历史群发记录”三个入口。
- 前两个入口共用安全、只读的增强 Markdown 阅读器；内容完全由 Agent 快照提供，CRM 不拆业务字段。
- 历史群发记录只读取当前任务显式绑定的 AI 助手 `plan_id`，点击后进入现有 AI 助手计划详情。
- 页面不提供编辑、审批、发送、重试、暂停或删除能力，不产生真实外部动作。
- 回滚方式为回退本 PR；历史快照仍保持可被旧模型读取，新增文档字段为可选默认空值。
