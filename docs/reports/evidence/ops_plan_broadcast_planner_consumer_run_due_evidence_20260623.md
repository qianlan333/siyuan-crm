# Ops Plan Broadcast Planner Consumer Run-Due Evidence - 2026-06-23

Verdict: `EVIDENCE_COLLECTED_NOT_READY`

This report records an approved production operator-window execution for the
target `ops_plan.approved` event's `broadcast_task_planner_consumer`.

The single-consumer preview succeeded and showed `would_execute=true`. The
approved single-consumer execution also succeeded at the worker-control level,
but the consumer completed as `skipped` with reason
`broadcast_task_planner_not_configured`. No `broadcast_job`,
`external_effect_job`, or Push Center projection was generated.

This is useful evidence, but it does not prove the full E2E business loop:

```text
ops_plan.approved
-> broadcast_task_planner_consumer
-> broadcast_job / external_effect_job
-> Push Center projection
```

## Scope

| Field | Evidence |
| --- | --- |
| Environment | production |
| Review date | 2026-06-23 |
| Plan id | `external_daily_lesson_20260617_1230_huangyoucan_v1_b11` |
| Redacted internal event id | `iev_***fd6b` |
| Consumer | `broadcast_task_planner_consumer` |
| Execution mode | single-consumer preview, then single-consumer execute |
| Broad run-due used | `false` |
| Batch consumer execution used | `false` |
| Route owner | `ai_crm_next` |

## Safety Attestation

| Field | Result |
| --- | --- |
| Runtime code changed | `false` |
| Route added or changed | `false` |
| Production deploy/systemd/nginx/env modified | `false` |
| Production migration executed | `false` |
| Broad run-due executed | `false` |
| Batch consumer execution used | `false` |
| Target consumer executed | `true` |
| Other consumers executed | `false` |
| External effect triggered | `false` |
| Real external call executed | `false` |
| Sensitive token/header/identifier committed | `false` |

## Single-Consumer Preview Evidence

Preview request:

```text
POST /api/admin/internal-events/{event_id}/consumers/broadcast_task_planner_consumer/run
dry_run=true
```

Preview result:

| Field | Evidence |
| --- | --- |
| HTTP status | `200` |
| `ok` | `true` |
| `dry_run` | `true` |
| `would_execute` | `true` |
| `real_external_call_executed` | `false` |
| candidate_count | `1` |
| processed_count | `0` |
| skipped_count | `0` |

The preview confirmed the target consumer was executable and that preview did
not write or call external systems.

## Single-Consumer Execution Evidence

Execution request:

```text
POST /api/admin/internal-events/{event_id}/consumers/broadcast_task_planner_consumer/run
dry_run=false
```

Execution result:

| Field | Before | After |
| --- | --- | --- |
| consumer status | `pending` | `skipped` |
| attempt count | `0` | `1` |
| HTTP status | n/a | `200` |
| worker `ok` | n/a | `true` |
| attempt status | n/a | `skipped` |
| real external call executed | `false` | `false` |
| processed_count | n/a | `1` |
| skipped_count | n/a | `1` |
| last_error | none | none |

Planner attempt evidence:

| Field | Evidence |
| --- | --- |
| redacted attempt id | `iea_***1424` |
| status | `skipped` |
| error_code | none |
| error_message | none |
| response reason | `broadcast_task_planner_not_configured` |
| response `skipped` | `true` |
| response external effect boundary | `handler_must_enqueue_external_effect_job_for_external_calls` |
| response `real_external_call_executed` | `false` |

## Broadcast Job Evidence

Read-only post-execution lookup for the plan id, trace id, and idempotency
patterns found no downstream broadcast job.

| Field | Evidence |
| --- | --- |
| `broadcast_job_id` | not found |
| generated `broadcast_jobs` count | `0` |
| downstream job expected after configured planner | yes |
| downstream job generated by this execution | `false` |

## External Effect / Push Center Evidence

No downstream external effect job was created by this planner execution.

| Field | Evidence |
| --- | --- |
| `external_effect_job_id` | not found |
| generated `external_effect_job` count | `0` |
| `push_center_job_id` | not found |
| `push_center_status` | `not_collected_no_broadcast_job` |
| Push Center projection formed | `false` |

This means the E2E path still stops at the internal-event consumer boundary.

## Sensitive-Data Redaction Evidence

Confirmed not committed:

- token
- secret
- `Authorization` header
- raw `external_userid`
- phone number
- raw target list
- raw member identifier
- raw customer identifier
- customer private request/response body

The report records only the plan id, redacted event id, consumer name, status,
counts, route names, and non-secret response summaries.

## Result

Result: `EVIDENCE_COLLECTED_NOT_READY`

Reasoning:

- The target planner consumer preview was valid and safe.
- The target planner consumer was executed exactly once.
- The consumer did not fail; it skipped intentionally with
  `broadcast_task_planner_not_configured`.
- No `broadcast_job` was created.
- No `external_effect_job` was created.
- No Push Center projection exists for the approval chain.

This cannot be promoted to `EVIDENCE_COLLECTED`,
`PASS_WITH_NOTES`, or `PASS_90_PLUS_CANDIDATE`.

## Why Not Global PASS_90_PLUS Yet

Global `PASS_90_PLUS` is not allowed because Ops Plan -> Broadcast E2E still
lacks the business job and Push Center visibility evidence. The consumer
execution proved the current implementation is safe and non-calling, but also
proved that the planner is not configured to generate the downstream job.

## Risk / Rollback

This PR is documentation-only after the approved operator evidence collection.
The production action performed was a single-consumer execution that moved the
target consumer from `pending` to `skipped` and recorded one skipped attempt.

Runtime rollback is not part of this PR. If the skipped consumer state must be
replayed later, use the existing retry/force operator path only after an
approved repair PR enables a real planner implementation.

## Next Action

Recommended next PR:

```text
Ops Plan -> Broadcast planner runtime repair
```

That PR should replace the currently skipped
`broadcast_task_planner_consumer` behavior with a real Next-native planner that
creates or reuses an idempotent `broadcast_job`, without direct external calls
from the API layer and without bypassing the existing internal-event,
background-job, DB/session, and external-effects gates.
